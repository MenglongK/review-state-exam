server:
  port: 2222
spring:
  application:
    name: exercise
  datasource:
    url: jdbc:postgresql://localhost:5432/review_exam
    driver-class-name: org.postgresql.Driver
    username: luxe
    password: luxe168

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: https://identity.istad.co/realms/app


